export class Inscripcion {
    constructor(plan, usuario, Estado // activa, inactiva
    ) {
        this.plan = plan;
        this.usuario = usuario;
        this.Estado = Estado;
    }
}
//# sourceMappingURL=inscripcion.model.js.map